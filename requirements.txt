# PropIQ Track Backend — Railway deployment (slim, production-only)

fastapi==0.110.1
uvicorn[standard]==0.25.0
starlette==0.37.2

# Database
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0

# Data validation
pydantic==2.12.5

# HTTP clients
httpx==0.28.1
requests==2.33.1

# Web scraping
beautifulsoup4==4.14.3
soupsieve==2.8.3
lxml==5.3.0

# Env loading
python-dotenv==1.2.2

# Google Sign-In: verifying ID tokens
google-auth==2.49.1

# Apple Sign-In: verifying Apple identity tokens (JWT w/ RSA signature)
PyJWT[crypto]==2.12.1
cryptography==46.0.7

# Form/multipart handling
python-multipart==0.0.24

# TLS + utilities
certifi==2026.2.25

# Emergent LLM integration (hosted on Emergent's public CloudFront pypi index).
# Requires EMERGENT_LLM_KEY env var to be set on Railway for AI Insights to work.
--extra-index-url https://d33sy5i8bnduwe.cloudfront.net/simple/
emergentintegrations==0.1.0

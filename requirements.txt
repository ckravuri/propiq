# PropIQ Track Backend — Railway deployment
# Slim dependency set (only what server.py actually imports)

fastapi==0.110.1
uvicorn[standard]==0.25.0
starlette==0.37.2

# Database
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0

# Data validation
pydantic==2.12.5

# HTTP clients
httpx==0.28.1
requests==2.33.1

# Web scraping (property lookup, address autocomplete)
beautifulsoup4==4.14.3
soupsieve==2.8.3
lxml==5.3.0

# Env loading
python-dotenv==1.2.2

# Form/multipart handling
python-multipart==0.0.24

# Optional: Emergent LLM integration (used when EMERGENT_LLM_KEY is set)
emergentintegrations==0.1.0

# TLS + utilities
certifi==2026.2.25

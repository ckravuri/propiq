# PropIQ Track Backend — Railway deployment (slim, production-only)

fastapi==0.110.1
uvicorn[standard]==0.25.0
starlette==0.37.2

# Database
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0

# Data validation
pydantic==2.12.5

# HTTP clients
httpx==0.28.1
requests==2.33.1

# Web scraping
beautifulsoup4==4.14.3
soupsieve==2.8.3
lxml==5.3.0

# Env loading
python-dotenv==1.2.2

# Google Sign-In: verifying ID tokens
google-auth==2.49.1

# Apple Sign-In: verifying Apple identity tokens (JWT w/ RSA signature)
PyJWT[crypto]==2.12.1
cryptography==46.0.7

# Form/multipart handling
python-multipart==0.0.24

# TLS + utilities
certifi==2026.2.25

# Emergent LLM integration (hosted on Emergent's public CloudFront pypi index).
# Kept as a fallback path inside /api/ai/insights — only used if GEMINI_API_KEY is unset
# or Gemini fails. Safe to leave EMERGENT_LLM_KEY unset on Railway once Gemini is live.
--extra-index-url https://d33sy5i8bnduwe.cloudfront.net/simple/
emergentintegrations==0.1.0

# Google Gemini (PRIMARY AI provider as of v1.0.11).
# Free tier: gemini-2.5-flash, 1500 req/day, 1M tokens/day, 15 RPM. No credit card.
# Set GEMINI_API_KEY env var on Railway. Get key at https://aistudio.google.com/app/apikey
google-genai==1.71.0
